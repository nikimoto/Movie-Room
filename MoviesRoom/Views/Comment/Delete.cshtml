@model MoviesRoom.Models.Comment

@{
    ViewBag.Title = "Delete comment";
}

<h2>@ViewBag.Title</h2>

<h3>Are you sure you want to delete this comment?</h3>
<fieldset>
    <legend>Comment</legend>
    <dl>
        <dt>
            @Html.DisplayNameFor(model => model.Film.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Film.Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.User.UserName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.User.UserName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Text)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Text)
        </dd>

    </dl>
</fieldset>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        <input type="submit" value="Delete" class="btn" />
        @Html.ActionLink("Cancel", "SingleFilm", "Home", new { id = Model.FilmId }, new { @class = "btn" })
    </div>
}
